[package]
name = "rustyraftlab"
version = "0.1.0"
edition = "2021"
description = "A Raft implementation in Rust with a deterministic fault-injection simulator and extensive correctness tests"
readme = "README.md"

[dependencies]
rocksdb = "0.24.0"

[dev-dependencies]
tempfile = "3.27.0"
